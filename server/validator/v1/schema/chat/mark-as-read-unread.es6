
const schema = {
  type: "object",
  required: [],
  properties: {
    id: {
      format: "number"
    },
    chat_id: {
      format: "number"
    },
    user_id: {
      format: "number"
    }
  }
};

export default schema;